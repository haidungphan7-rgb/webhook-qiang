/**
 * Declarations for globals injected by `vite.config.ts` via `define`.
 *
 * Empty on purpose: `__GITHUB_PROJECT_LINK__` and `__LATEST_RELEASE_LINK__` used to be
 * declared here while pointing at the UPSTREAM repository, so this app would have sent
 * its own users to somebody else's project. Nothing in src/ referenced them, so both the
 * declarations and the `define` block were removed rather than repointed.
 */
